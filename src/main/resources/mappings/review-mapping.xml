<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ReviewMapper">
	
	<!--리뷰 페이징  -->
	<select id="reviewListwithPaging" parameterType="hashmap" resultType="review">
	  <![CDATA[
	  SELECT re_\seq, bseq,content,score,indate,reply
	  FROM (SELECT row_number() OVER (ORDER BY indate DESC) rn, reseq, re.bseq,email
	        content,score,indate,reply FROM review_view
	        WHERE bseq=#{bseq})
	        WHERE rn <= #{criteria.pageNum} * #{criteria.rowsPerPage}
	        AND rn > (#{criteria.pageNum}-1) * {criteria.rowsPerPage}
	   ]]>
	
	</select>
	<!-- 리뷰 등록 -->
	<insert id="insertReview">
		INSERT INTO review(reseq,  bseq, content, score, reply)
		VALUES(review_seq.NEXTVAL, #{bseq}, #{content}, #{score}, #{reply})
	</insert>
	
	<!-- 객실번호로 리뷰 조회 -->
	<select id="selectReviewByRseq" resultType="review">
		SELECT * FROM review_view
		WHERE rseq=#{rseq}
	</select>
	
	<!-- 리뷰수정 -->
	<update id="updateReview">
		UPDATE review SET content=#{content}, score=#{score}
		WHERE reseq=#{reseq}
	</update>
	
	<!-- 리뷰삭제 -->
	<delete id="deleteReview">
		DELETE review WHERE reseq=#{reseq}
	</delete>
	
	<!-- 전체 리뷰 조회 -->
	<select id="getListReview" resultType="review">
		SELECT * FROM review ORDER BY reseq DESC
	</select>
	
	<!-- 리뷰 답글 달기 -->
	<update id="insertReply">
		UPDATE review SET reply=#{reply}
		WHERE reseq=#{reseq}
	</update>
	
	<select id="countReviewList" resultType="int">
	SELECT COUNT(*) FROM review WHERE bseq=#{bseq}
    </select>
    
    
    <select id="getEmailView" resultType= "review">   
    SELECT  email  FROM review_view
   </select>
   
   
   <select id="avgScore" resultType= "review">
  select avg(score) from review where rseq=#{rseq}
   </select>
   
   
</mapper>
